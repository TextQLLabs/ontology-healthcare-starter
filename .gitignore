.DS_Store
*.log
# never commit credentials or licensed code-set bulk files
credentials*
*.secret
licensed-codesets/
# loader scratch dir (the built CSVs are committed up one level in reference/terminology/)
reference/terminology/_build/
